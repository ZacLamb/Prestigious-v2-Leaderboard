Drop the Prestigious Capital Group logo here, named exactly: logo.png

The header loads /logo.png. If it's missing the image hides itself and the
text title shows alone, so the dashboard still works without it.
